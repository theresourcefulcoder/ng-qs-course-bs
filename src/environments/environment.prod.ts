export const environment = {
  production: true,
  version: '1.0.0', /* Application version displayed in the footer - prod mode */
  timeout: 15, // in minutes
  timeoutCheckInterval: 5000, // in milliseconds
  serverApiPath: 'http://localhost:8000/api/'
};
